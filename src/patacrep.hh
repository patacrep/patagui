#ifndef CPATACREP_H
#define CPATACREP_H

#include <QObject>

class Patacrep : public QObject
{
    Q_OBJECT
public:
    explicit Patacrep(QObject *parent = 0);

signals:

public slots:
};

#endif // CPATACREP_H
